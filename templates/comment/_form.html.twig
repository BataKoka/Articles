{{ form_start(form) }}

    {{ form_widget(form) }}

    <div class="form-group row">
        <div class="col-sm-2"></div>
        <div class="col-sm-8">
            <button class="btn btn-primary">{{ button_label|default('Save') }}</button>
        </div>
        <div class="col-sm-2">
            <a href="{{ path('article_show', {'id': articleId}) }}" class="btn btn-outline-info">back to article</a>
        </div>
    </div>

{{ form_end(form) }}